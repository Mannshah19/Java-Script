let totalMinutes: number = 140;

let hours: number = Math.floor(totalMinutes / 60);
let minutes: number = totalMinutes % 60;

console.log(`${totalMinutes} minutes = ${hours} hours and ${minutes} minutes`);
